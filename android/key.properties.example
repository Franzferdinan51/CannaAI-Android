# Key store configuration for release builds
# Copy this file to 'key.properties' and fill in your actual values
# IMPORTANT: Never commit the actual key.properties file to version control

storePassword=<your_keystore_password>
keyPassword=<your_key_password>
keyAlias=<your_key_alias>
storeFile=<path_to_your_keystore_file>  # e.g., ../keystore.jks

# Example configuration:
# storePassword=mySecretPassword123
# keyPassword=myKeyPassword456
# keyAlias=cannaai-release-key
# storeFile=../cannaai-release-keystore.jks